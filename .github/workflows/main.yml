on: 
  push:
   branchaes:
      - main
      
name: deploy website on push
jobs:
  web-deploy:
     name: Deploy
     runs-on: ubantu-latest
     steps:
     - name: Get latest code
       uses: actions/checkout@v2.3.2

     - name: Sync files
       uses: SamKirkland/FTP-Deploy-Action@4.0.0
       with:
         server: ftp.netelmart.net
         username: devftp@dev.netelmart.net
         password: gzsHiwRdR#^x
         server-dir: /home/neteklzv/dev.netelmart.net

  
